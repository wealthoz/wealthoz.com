$('#balance').html("<%= j render('transparent/report_balance') %>")
$('#end_date').datepicker({
                      weekStart: 1,
                      autoclose: true,
                      format:'yyyy-mm-dd' ,
                      todayHighlight: true
                    });
