  <div class="span8">
    <% if @user.projects.any? %>
      <h4>Projects <span class="badge"><%= @user.projects.count %></span></h4>
      <ol class="projects">
        <%= render @projects %>
      </ol>
      
    <% end %>
  </div>
