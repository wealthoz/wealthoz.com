<%= form_for(@group) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Create Your Wealth Group!" %>
  </div>
  <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>