<% provide(:title, 'Home') %>

<% if signed_in? %>
  <div class="row">
    <aside class="col-xs-3">
      <section>
        <%= render 'shared/user_info' %>
      </section>
       <section>
        <%= render 'shared/stats' %>
      </section>

      <section>
        <%= render 'shared/micropost_form' %>
      </section>
      <section>

      </section>
    </aside>
    <div class="col-xs-5">
      <h4>FuturePosts Feed</h4>
      <%= render 'shared/feed' %>
    </div>


  </div>
<% else %>

<div class="center jumbotron">

	<%= render 'shared/video_youtube' %>

	<h3>"Wealth OZ" a financial app that solves the two most
		difficult and important financial problems out there."
	</h3>
	<%= link_to "Sign up it's free!", signup_path , class: "btn btn-success btn-lg" %>
	<h3>Problem A</h3>
	<p class="text-warning">People do not know their WEALTH !!!</p>

	<h3>Problem B</h3>
	<p class="text-warning">People do not know their GROUP WEALTH !!!</p>


</div>
<% end %>
