<a class="btn btn-success assets" href="#" rel="tooltip" data-placement="bottom"
title="Increase(+) Decrease(-)">Assets<span class="badge" >0</span></a>
<a class="btn btn-warning debt" href="#" rel="tooltip" data-placement="bottom"
title="Increase(-) Decrease(+)">-Debt<span class="badge">0</span></a>
<a href="#">=</a>
<a class="btn btn-success income" href="#" rel="tooltip" data-placement="bottom"
title="Increase(+) Decrease(-)">Income<span class="badge">0</span></a>
<a class="btn btn-warning expenses" href="#" rel="tooltip" data-placement="bottom"
title="Increase(-) Decrease(+)">-Expenses<span class="badge">0</span></a>
<a href="#">=</a>
<a class="btn btn-default" href="#" rel="tooltip" data-placement="bottom"
title="Increase(+) Decrease(-)"><strong>Wealth</strong><span class="wealth badge">0</span></a>
%a.hide.accumulated_wealth
  %span.badge


= form_tag(budgets_path, remote: true, id: 'new-budget-form' ) do
  %table{:class => "table table-striped table-hover "}
    %tr
      %th *Account
      %th= render 'shared/help_wunits'
      %th *Date y-m-d
      %th *Amount
      %th Text
      %th Action

    %tr.new-budget-row
      = render 'budget_fields'

    %tr.actions
      %td= submit_tag 'POST',class: "btn btn-success", id: 'new-budget-form-submit'
      %td= link_to 'Add Row', '#', id: '_form',class: "btn "
      %td= render 'shared/transaction_help'
      %td{colspan: 3} Unbalanced Amount<span class="unbalanced-amount badge">0</span>
      %td
      %td

    %tr.new_budget_row.hidden{:class => ""}
      = render 'budget_fields'
%p *Required fields
%p{:class => "text-success"} In order to POST a transaction all required fields must be Green
%p{:class => "text-success"} and the Unbalanced Amount must be Zero
