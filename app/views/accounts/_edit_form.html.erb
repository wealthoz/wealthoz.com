<%= form_for(@account) do |f| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% @account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name,"New Account Name" %><br>
    <%= f.text_field :name %>
    

  </div>
 <div class="actions">
    <%= f.submit "Save", class: "btn btn-large btn-primary" %>
  </div>
<% end %>

<h5 class="text-warning">You can change only <strong>the name of the Account </strong> !!!</h5>
<h5 class="text-warning"><strong>
	Changing the name of the Account will change it across all your booked Transactions!!!
</strong></h5>
	

<h5 class="text-warning">You cannot change the report class this account belongs (e.g. Assets,Debt,Income, Expenses)!!!</h5>
